# ==========================================
# LOCAL DEVELOPMENT CONFIGURATION
# ==========================================
# This .env file is for LOCAL development only
# Railway uses environment variables set in the dashboard (not this file)

# Django Settings (for local dev)
SECRET_KEY="your-secret-key-here-change-this-in-production"
DEBUG="True"
ALLOWED_HOSTS="*"

# Database for Local Development
# Uncomment to use SQLite locally (easier for development):
# DATABASE_URL="sqlite:///db.sqlite3"

# OR use this Supabase connection for local testing:
DATABASE_URL="postgresql://postgres:Bbatombari%231212@db.adqdddhtsamyqvmxywem.supabase.co:5432/postgres"

# CORS Settings (for local dev + production frontend)
CORS_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,https://upsn-website.vercel.app"

# JWT Settings
ACCESS_TOKEN_LIFETIME_MINUTES="300"
REFRESH_TOKEN_LIFETIME_DAYS="1"

# ==========================================
# RAILWAY PRODUCTION CONFIGURATION
# ==========================================
# Set these in Railway Dashboard → Backend Service → Variables tab:
#
# DATABASE_URL         = (automatically provided when you add PostgreSQL database)
# SECRET_KEY           = django-insecure-generate-a-new-secure-key-here
# DEBUG                = False
# ALLOWED_HOSTS        = .railway.app,.up.railway.app
# CORS_ALLOWED_ORIGINS = http://localhost:5173,https://upsn-website.vercel.app
#
# Note: Railway will automatically inject DATABASE_URL when you add Postgres
# ==========================================
